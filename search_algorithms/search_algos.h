#ifndef VARIA_FUNC_PROTO
#define VARIA_FUNC_PROTO

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>

int linear_search(int *array, size_t size, int value);
int binary_search(int *array, size_t size, int value);

#endif
